[project]
name = "agent-runtime-python"
version = "0.1.0"
description = "Python"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "jm5905938", email = "jm5905938@gmail.com" }
]
dependencies = []

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "agent_runtime"
module-root = "src"
